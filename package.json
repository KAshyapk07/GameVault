{
  "name": "webtech-project",
  "version": "1.0.0",
  "description": "Full stack project with admin, backend, and frontend",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:admin\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "start:admin": "cd admin && npm run dev",
    "build": "npm run build:frontend && npm run build:admin",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:admin": "cd admin && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "author": "",
  "license": "ISC"
}
